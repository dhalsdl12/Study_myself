#include <stdio.h>

int main()
{
	//getchar(), putchar()
	//한문자만 입력시 사용
	//buffer : 데이터의 임시저장 공간
	char i, j, k;

	i = getchar();
	putchar(i);
	getchar();

	j = getchar();
	putchar(j);
	getchar();

	k = getchar();
	putchar(k);
	
}